port: 7880
bind_addresses:
  - "0.0.0.0"
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true
  enable_loopback_candidate: false
redis:
  address: 127.0.0.1:6379
  password: "{{ .RedisPassword }}"
turn:
  enabled: false
  domain: turn.dtalk.remvn.dev
  tls_port: 5349
  udp_port: 3478
  external_tls: true
keys:
  {{ .LiveKitApiKey }}: "{{ .LiveKitApiSecret }}"
